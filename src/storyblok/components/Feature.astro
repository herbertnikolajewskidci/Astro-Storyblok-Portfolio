---
import { storyblokEditable } from "@storyblok/astro";

const { blok } = Astro.props;
---

<div
    {...storyblokEditable(blok)}
    class="w-full p-12 bg-primary-100 rounded-[5px] text-center"
>
    <h3 class="text-2xl text-primary-900 font-bold">{blok.name}</h3>
</div>
